name: Publish to PyPI
on:
  release:
    types: [published]
permissions:
  contents: read
jobs:
  validate:
    uses: ./.github/workflows/ci.yml
  validate-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - run: python scripts_dev/validate_release.py
        env:
          REF_NAME: ${{ github.event.release.tag_name }}
  pypi-publish:
    needs: [validate, validate-version]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: validated-dist-${{ github.sha }}
          path: dist/
      - name: Verify the exact tested bytes
        run: |
          sha256sum --check dist/SHA256SUMS
          rm dist/SHA256SUMS
      - uses: pypa/gh-action-pypi-publish@7f25271a4aa483500f742f9492b2ab5648d61011
