# Automated delivery skills

Use the canonical `.agents/workflows/review-delivery.md` workflow for the required
reviewer roster, test/UI promotion, bounded batches and structured evidence.
Use `.agents/EVIDENCE.md` for report fields, snapshot freshness and device sign-off.

The lead selects applicable competency-matrix rows and project references before
implementation. Diagnostics remains on-demand. No fixed reviewer count or successful
install alone establishes delivery. Report the actual verification performed.
